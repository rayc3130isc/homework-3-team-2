# homework-3-team-2

Box<T> is considered highly reusable because it uses generics to act as a placeholder for any reference type, allowing a single class definition to handle diverse data types while maintaining type safety.  Without generics, a Box class would be restricted to a specific type, requiring massive code duplication to handle different types. 

An Iterator does not need casting in modern programming languages because generics are used to specify the type of elements it will return between the diamond brackets. 

Every single time that you create an ArrayList is erased the generic time at runtime and is stored as an object type object time and the compiler enforces type safety and adds casts when you retrieve elements.


